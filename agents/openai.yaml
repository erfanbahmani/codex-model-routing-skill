interface:
  display_name: "Codex Model Routing"
  short_description: "Strongest-model manager, lean lower-model execution"
  default_prompt: "Use $codex-model-routing to keep the strongest model in charge and minimize total task tokens."
